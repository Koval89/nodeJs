export enum StatusCodesEnum{
    OK=200,  // на всі гет
    CREATED=201, //при створенні
    NO_CONTENT=204,//при видаленні але джейсонку ми не передаємо
    BED_REQUEST=400, //використовуємо при валідації коли користувач хоче щось не те відправити до БД
    UNAUTHORIZED=401,// коли не авторизований користувач
    FORBIDDEN=403,// коли доступ у користувача який залогований заблокований
    NOT_FOUND=404 //коли користувач шукає щось чого немає в базі даних
}